# GitHub Repository Metadata

This document records suggested public repository metadata for review hygiene only. It has no runtime, governance, release, CI, or packaging authority.

Suggested description:

> Deterministic governance and evidence layer for bounded AI-agent actions.

Suggested topics:

- ai-governance
- agent-safety
- deterministic-governance
- capability-tokens
- evidence-ci
- auditability
- fail-closed

These values must be set manually in GitHub. GateGraph does not auto-update repository metadata.
