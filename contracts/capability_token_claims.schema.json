{
  "description": "Descriptive claim contract for signed capability tokens; token verification logic remains in src/capability_token.py.",
  "non_scope": [
    "secret resolution",
    "runtime execution",
    "capability amplification"
  ],
  "required_fields": {
    "actor_id": "string",
    "budget_scope_id": "string_or_null",
    "capabilities": "object",
    "expires_at": "string",
    "kid": "string",
    "signature": "string",
    "task_id": "string",
    "token_id": "string"
  },
  "schema_version": "0.14.4",
  "security": {
    "audit_logging": "log token_id/token_hash only; never log full token values",
    "replay_boundary": "token must remain bound to task/scope claims"
  },
  "status": "frozen_stable",
  "surface": "capability_token_claims"
}
