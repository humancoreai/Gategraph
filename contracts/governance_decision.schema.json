{
  "schema_version": "0.12.1",
  "surface": "governance_decision",
  "status": "frozen_stable",
  "description": "Descriptive contract for governance decision records; not executable policy.",
  "required_fields": {
    "decision_id": "string",
    "task_id": "string",
    "decision": "string",
    "risk_level": "string",
    "selected_rule_id": "string_or_null",
    "matched_rule_ids": "array[string]",
    "token_issued": "boolean",
    "event_id": "string",
    "schema_version": "string"
  },
  "allowed_values": {
    "decision": ["allow", "block", "review", "require_approval"],
    "risk_level": ["low", "medium", "high", "critical"]
  },
  "non_scope": ["runtime authority", "policy mutation", "semantic scoring"]
}
